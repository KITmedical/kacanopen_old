# Don't commit content generated by Doxygen.
html/
# Dont't commit local changes to .gitignore. You can use "git config --global core.excludesfile" instead or uncomment the following line:
# .gitignore
# Build is either done with Catkin or in a user-defined dictionary using CMake so there are no binary files to ignore in public repo.
